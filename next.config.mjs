/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "plus.unsplash.com"], // Add any other trusted domains here
  },
};

export default nextConfig;
